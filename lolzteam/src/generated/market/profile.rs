// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, ParamValue};
use super::types::*;

pub struct ProfileApi {
	http: Arc<HttpClient>,
}

impl ProfileApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Claims
	pub async fn claims(&self, params: Option<&ProfileClaimsParams>) -> Result<ProfileClaimsResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.r#type {
				query.push(("type".into(), ParamValue::String(v.clone())));
			}
			if let Some(ref v) = p.claim_state {
				query.push(("claim_state".into(), ParamValue::String(v.clone())));
			}
		}
		self.http.request("GET", "/claims", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Profile
	pub async fn get(&self, params: Option<&ProfileGetParams>) -> Result<ProfileGetResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.fields_include {
				for item in v {
					query.push(("fields_include".into(), ParamValue::String(item.to_string())));
				}
			}
		}
		self.http.request("GET", "/me", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Edit Market Settings
	pub async fn edit(&self, body: Option<&ProfileEditBody>) -> Result<ProfileEditResponse, LolzteamError> {
		self.http.request_json("PUT", "/me", None, body).await
	}
}
