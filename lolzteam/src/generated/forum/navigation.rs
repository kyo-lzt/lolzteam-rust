// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, ParamValue};
use super::types::*;

pub struct NavigationApi {
	http: Arc<HttpClient>,
}

impl NavigationApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Navigation
	pub async fn list(&self, params: Option<&NavigationListParams>) -> Result<NavigationListResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.parent {
				query.push(("parent".into(), ParamValue::Integer(*v)));
			}
		}
		self.http.request("GET", "/navigation", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}
}
