// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, ParamValue};
use super::types::*;

pub struct ChatboxApi {
	http: Arc<HttpClient>,
}

impl ChatboxApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Chats
	pub async fn index(&self, params: Option<&ChatboxIndexParams>) -> Result<ChatboxIndexResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.room_id {
				query.push(("room_id".into(), ParamValue::Integer(*v)));
			}
		}
		self.http.request("GET", "/chatbox", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Unignore Chat User
	pub async fn delete_ignore(&self, body: Option<&ChatboxDeleteIgnoreBody>) -> Result<ChatboxDeleteIgnoreResponse, LolzteamError> {
		self.http.request_json("DELETE", "/chatbox/ignore", None, body).await
	}

	/// Get Ignored Chat Users
	pub async fn get_ignore(&self) -> Result<ChatboxGetIgnoreResponse, LolzteamError> {
		self.http.request("GET", "/chatbox/ignore", None, None).await
	}

	/// Ignore Chat User
	pub async fn post_ignore(&self, body: Option<&ChatboxPostIgnoreBody>) -> Result<ChatboxPostIgnoreResponse, LolzteamError> {
		self.http.request_json("POST", "/chatbox/ignore", None, body).await
	}

	/// Delete Chat Message
	pub async fn delete_message(&self, body: Option<&ChatboxDeleteMessageBody>) -> Result<ChatboxDeleteMessageResponse, LolzteamError> {
		self.http.request_json("DELETE", "/chatbox/messages", None, body).await
	}

	/// Get Chat Messages
	pub async fn get_messages(&self, room_id: i64, params: Option<&ChatboxGetMessagesParams>) -> Result<ChatboxGetMessagesResponse, LolzteamError> {
		let mut query = Vec::new();
		query.push(("room_id".into(), ParamValue::Integer(room_id)));
		if let Some(p) = params {
			if let Some(ref v) = p.before_message_id {
				query.push(("before_message_id".into(), ParamValue::Integer(*v)));
			}
		}
		self.http.request("GET", "/chatbox/messages", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Create Chat Message
	pub async fn post_message(&self, body: Option<&ChatboxPostMessageBody>) -> Result<ChatboxPostMessageResponse, LolzteamError> {
		self.http.request_json("POST", "/chatbox/messages", None, body).await
	}

	/// Edit Chat Message
	pub async fn edit_message(&self, body: Option<&ChatboxEditMessageBody>) -> Result<ChatboxEditMessageResponse, LolzteamError> {
		self.http.request_json("PUT", "/chatbox/messages", None, body).await
	}

	/// Get Chat Leaderboard
	pub async fn get_leaderboard(&self, params: Option<&ChatboxGetLeaderboardParams>) -> Result<ChatboxGetLeaderboardResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.duration {
				query.push(("duration".into(), ParamValue::String(v.clone())));
			}
		}
		self.http.request("GET", "/chatbox/messages/leaderboard", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Chat Online
	pub async fn online(&self, room_id: i64) -> Result<ChatboxOnlineResponse, LolzteamError> {
		let mut query = Vec::new();
		query.push(("room_id".into(), ParamValue::Integer(room_id)));
		self.http.request("GET", "/chatbox/messages/online", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Chat Message Report Reasons
	pub async fn report_reasons(&self, message_id: i64) -> Result<ChatboxReportReasonsResponse, LolzteamError> {
		let mut query = Vec::new();
		query.push(("message_id".into(), ParamValue::Integer(message_id)));
		self.http.request("GET", "/chatbox/messages/report", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Report Chat Message
	pub async fn report(&self, body: Option<&ChatboxReportBody>) -> Result<ChatboxReportResponse, LolzteamError> {
		self.http.request_json("POST", "/chatbox/messages/report", None, body).await
	}
}
