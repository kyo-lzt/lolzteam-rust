// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, MultipartPart};
use super::types::*;

pub struct OAuthApi {
	http: Arc<HttpClient>,
}

impl OAuthApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Access Token
	pub async fn token(&self, body: Option<&OAuthTokenBody>) -> Result<OAuthTokenResponse, LolzteamError> {
		let mut parts = Vec::new();
		if let Some(b) = body {
			if let Some(ref v) = b.client_id {
				parts.push(MultipartPart::Text {
					name: "client_id".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.client_secret {
				parts.push(MultipartPart::Text {
					name: "client_secret".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.grant_type {
				parts.push(MultipartPart::Text {
					name: "grant_type".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.scope {
				parts.push(MultipartPart::Text {
					name: "scope".to_string(),
					value: v.join(" "),
				});
			}
			if let Some(ref v) = b.code {
				parts.push(MultipartPart::Text {
					name: "code".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.redirect_uri {
				parts.push(MultipartPart::Text {
					name: "redirect_uri".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.refresh_token {
				parts.push(MultipartPart::Text {
					name: "refresh_token".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.password {
				parts.push(MultipartPart::Text {
					name: "password".to_string(),
					value: v.clone(),
				});
			}
			if let Some(ref v) = b.username {
				parts.push(MultipartPart::Text {
					name: "username".to_string(),
					value: v.clone(),
				});
			}
		}
		self.http.request_multipart("POST", "/oauth/token", parts).await
	}
}
