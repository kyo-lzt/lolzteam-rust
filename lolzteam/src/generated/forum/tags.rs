// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, ParamValue};
use super::types::*;

pub struct TagsApi {
	http: Arc<HttpClient>,
}

impl TagsApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Popular Tags
	pub async fn popular(&self) -> Result<TagsPopularResponse, LolzteamError> {
		self.http.request("GET", "/tags", None, None).await
	}

	/// Get Filtered Content
	pub async fn find(&self, params: Option<&TagsFindParams>) -> Result<TagsFindResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.tag {
				query.push(("tag".into(), ParamValue::String(v.clone())));
			}
		}
		self.http.request("GET", "/tags/find", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Tags
	pub async fn list(&self, params: Option<&TagsListParams>) -> Result<TagsListResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.page {
				query.push(("page".into(), ParamValue::Integer(*v)));
			}
			if let Some(ref v) = p.limit {
				query.push(("limit".into(), ParamValue::Integer(*v)));
			}
		}
		self.http.request("GET", "/tags/list", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Tagged Content
	pub async fn get(&self, tag_id: i64, params: Option<&TagsGetParams>) -> Result<TagsGetResponse, LolzteamError> {
		let path = format!("/tags/{tag_id}");
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.page {
				query.push(("page".into(), ParamValue::Integer(*v)));
			}
			if let Some(ref v) = p.limit {
				query.push(("limit".into(), ParamValue::Integer(*v)));
			}
		}
		self.http.request("GET", &path, if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}
}
