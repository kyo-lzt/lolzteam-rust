// Generated by codegen — do not edit manually

use std::sync::Arc;

use crate::runtime::{HttpClient, LolzteamError, ParamValue};
use super::types::*;

pub struct PagesApi {
	http: Arc<HttpClient>,
}

impl PagesApi {
	pub(crate) fn new(http: Arc<HttpClient>) -> Self {
		Self { http }
	}

	/// Get Pages
	pub async fn list(&self, params: Option<&PagesListParams>) -> Result<PagesListResponse, LolzteamError> {
		let mut query = Vec::new();
		if let Some(p) = params {
			if let Some(ref v) = p.parent_page_id {
				query.push(("parent_page_id".into(), ParamValue::Integer(*v)));
			}
			if let Some(ref v) = p.order {
				query.push(("order".into(), ParamValue::String(v.clone())));
			}
		}
		self.http.request("GET", "/pages", if query.is_empty() { None } else { Some(query.as_slice()) }, None).await
	}

	/// Get Page
	pub async fn get(&self, page_id: i64) -> Result<PagesGetResponse, LolzteamError> {
		let path = format!("/pages/{page_id}");
		self.http.request("GET", &path, None, None).await
	}
}
